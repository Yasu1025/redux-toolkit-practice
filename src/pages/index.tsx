import React, { VFC } from "react";

const Home: VFC = () => {
  return (
    <div>
      <h2>Home</h2>
      {/** Search input */}
      {/** Cocktail list */}
    </div>
  );
};

export default Home;
