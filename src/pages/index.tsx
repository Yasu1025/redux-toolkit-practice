import React, { VFC } from "react";
import SearchInput from "../components/parts/SearchInput";

const Home: VFC = () => {
  return (
    <div>
      <SearchInput />
      {/** Cocktail list */}
    </div>
  );
};

export default Home;
